import { ButtonProps } from './type';
/**
 * Primary UI component for user interaction
 */
declare const Button: ({ variant, size, label, loading, customClass, fullwidth, typeStyle, iconRight, iconLeft, disabled, ...props }: ButtonProps) => import("react/jsx-runtime").JSX.Element;
export default Button;
