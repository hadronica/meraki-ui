import { SelectProps } from './type';
/**
 * Primary UI component for user interaction
 */
declare const Select: ({ options, value, onChange, label, readonly, name, error }: SelectProps) => import("react/jsx-runtime").JSX.Element;
export default Select;
