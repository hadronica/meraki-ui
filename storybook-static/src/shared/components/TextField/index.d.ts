import { TextFieldProps } from './type';
declare const TextField: ({ label, value, placeholder, iconLeft, size, iconRight, type, password, onChange, error, readonly, pattern, customMessageError, customClass }: TextFieldProps) => JSX.Element;
export default TextField;
