import { TextAreaProps } from './type';
declare const TextArea: ({ label, value, placeholder, size, onChange, error, readonly, customMessageError }: TextAreaProps) => JSX.Element;
export default TextArea;
